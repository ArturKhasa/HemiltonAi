/** @type {import('tailwindcss').Config} */

// Фирменный цвет панели одним местом. В разметке используется только `brand-*`
// (не `blue-*`/`green-*`), поэтому перекрасить интерфейс целиком — это правка
// значений ниже, а не полутора сотен классов по страницам.
//
// Цветные бейджи статусов (ChatPage → STATUS_COLORS) намеренно НЕ фирменные:
// «Поинтересовался» синий, «Заказ оформлен» зелёный, и они должны отличаться
// друг от друга и от кнопок при любой теме.
const brand = {
  50: '#ecfdf5',
  100: '#d1fae5',
  200: '#a7f3d0',
  300: '#6ee7b7',
  400: '#34d399',
  500: '#10b981',
  600: '#059669',
  700: '#047857',
  800: '#065f46',
  900: '#064e3b',
}

export default {
  content: ['./index.html', './src/**/*.{vue,js,ts}'],
  theme: {
    extend: {
      colors: { brand },
    },
  },
  plugins: [],
}
